/*
// exibe um campo do item salvo do async storage
const ReadByID = async (field: string) => {
    const data = await readDataByID("@login", field)
    return data || ""
}

// remove os items do async storage baseado no idTipo_Usuario
const Delete = async (idTipo_Usuario: number) => { 
    const data = await deleteData("@login", idTipo_Usuario)
    console.log(data)
}
*/